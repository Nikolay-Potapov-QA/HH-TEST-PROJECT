// проверка статус кода
pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});
// проверка того, что ключ 'areas' является пустым
pm.test("Field 'areas' is empty", function () {
    var jsonData = pm.response.json();
pm.expect(jsonData.areas).to.be.empty;
});
// проверка того, что поле area является массивом
pm.test("Field 'areas' is an array", function () {
    var jsonData = pm.response.json();
    pm.expect(jsonData).to.have.property('areas');
    pm.expect(jsonData.areas).to.be.an('array');
});