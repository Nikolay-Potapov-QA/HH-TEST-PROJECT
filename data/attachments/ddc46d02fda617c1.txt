// Проверка статус-кода
pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});
// Проверка времени ответа от сервера
pm.test("Response time is less than 200ms", function () {
    pm.expect(pm.response.responseTime).to.be.below(1000);
});
// Проверка на то, что корневой id строго равен 113
pm.test("Root ID is strictly equal to '113'", function () {
    var jsonData = pm.response.json();
    pm.expect(jsonData.id).to.eql("113");
});
// Проверка на то, что ответ от сервера является объектов а не массивом
pm.test("Response is an Object (not Array)", function () {
    var jsonData = pm.response.json();
    pm.expect(jsonData).to.be.an('object');
});